# 产品截图来源

截图日期：2026-09-23。全部通过浏览器截取 BeatMate 实际页面，封面尺寸为 1280 × 720，工作台尺寸为 1280 × 1350（为完整展示表单临时加高浏览器视口）；没有修改页面数据、拼接界面或合成文字。

- `cover-screen.jpg`：入口封面，包含已确认的两句产品文案。背景来源见 [封面记录](cover-provenance.md)。
- `studio-screen.jpg`：纯伴奏音乐描述，展开后的全部制作偏好与项目选择，曲库和底部播放器。
- `stems-screen.jpg`：完整歌词与制作要求同时可见，右侧展示整曲/伴奏/人声、MIDI 与本地打包入口；导出按钮和说明保留正确间距。

工作台数据由独立临时 Mock 服务产生，采用虚构作品名和示例歌词；所有音频均为 4 秒合成测试信号，MIDI 为测试音符，不作为真实服务质量示例。未读取个人曲库、未展示 API Key、未调用付费服务。

复拍时使用独立演示库，通过页面填写示例、展开音轨与选择播放器。保留 Mock 标记及真实时长，不将其改成真实音乐产物。封面应截取完整入口页面，而非直接引用不含 HTML 文案的背景原图。
