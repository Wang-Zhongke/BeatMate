# 人工试听实验（未执行）

A/B/C.json保存原始需求、Direct最终描述和人工评价表。模型、输出文件、返回模型、费用和评价均为空/unknown；这是实验准备，不是生成成功报告。

人工逐条显式运行smoke时，用对应raw_text作为 `--text`，主动确认模型/账户/费用。每条是单独的新付费请求，不能自动批量执行。将smoke报告中的任务、模型、输出文件和hash记录到对应评价表后再试听。填写情绪符合度、声音质感、旋律与留白、段落变化、是否愿意用于写词或录Rap；不要把测试信号当音乐质量样本。

当前工程Mock演示位于output/audio-mvp-demo，只是4秒合成测试信号，既不实现这三条音乐需求，也不声称达到Mureka水平。
